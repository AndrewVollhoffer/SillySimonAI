Currently a very simple script that allows you to create and call methods that will automatically input keystrokes.

Dubbed SIMONAI after a past coworker.

Should work on Windows and Linux while sudo user.
